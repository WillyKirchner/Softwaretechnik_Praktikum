spring.application.name=Software_praktikum

# Server
server.port=5000

# delete after creating db:
#spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

# Database Configuration (we dont have DB yet)
spring.datasource.url=jdbc:mariadb://localhost:3306/essenausgabe
spring.datasource.username=root
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration (for ORM, commented out since no DB)
# spring.jpa.hibernate.ddl-auto=none

# Logging level
logging.level.org.springframework=INFO
